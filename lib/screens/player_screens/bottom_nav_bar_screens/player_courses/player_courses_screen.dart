import 'package:flutter/material.dart';

class PlayerCoursesScreen extends StatelessWidget {
  const PlayerCoursesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
