import 'package:flutter/material.dart';

class PlayerArticlesScreen extends StatelessWidget {
  const PlayerArticlesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
